import 'package:flutter/material.dart';

class MenuSetsScreen extends StatelessWidget {
  const MenuSetsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Menu Sets'),
      ),
    );
  }
}
